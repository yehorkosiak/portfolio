export default function ProjectsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

export const metadata = {
  title: "Projects | Portfolio",
  description: "Detailed project showcases and technical implementations",
};
